/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ["upload.wikimedia.org", "platform-lookaside.fbsbx.com", "scontent.fmel14-1.fna.fbcdn.net"],
    }
}

module.exports = nextConfig
